# let VarName1:
#     input:
#         fn functionName:
#             input: point '/path/Point.Name/'

# queue queueApi:
#     max-length: 10000

task task1:
    cycle: 100
    apiQueue: queueApi
    let Var1:
        input point '/path/Point.Name/'
    fn sendTo apiQueue:
        input metric sqlSelectMetric:
            initial: 0.123      # начальное значение
            table: table_name
            sql: "UPDATE {table} SET kind = '{input1}' WHERE id = '{input2}';"    
            inputs:
                input1:
                    let Var2:
                        input fn sum:
                            input fn sum:
                                input1: const int 1
                                input2: point int '/path/Point.Name/'
                # input2:
                #     const int 1

# service ApiClient:

# service CmaClient:

# service CmaServer:

# service ExternalDevice:
#     protocol: profinet

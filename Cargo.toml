[package]
name = "fr-service"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "^0.4"
env_logger = "^0.10"

# num = "^0.4"
# derive-new = "^0.5"

serde = { version = "^1.0", features = ["derive"] }
erased-serde = "0.3.31"
serde_yaml = "^0.9"
serde_json = "^1.0"
typetag = "^0.2"

regex = "^1.9"

rand = "^0.8"

chrono = "0.4"

indexmap = "^2.0"

# [[bin]]
# name = "task_test"
# path = "src/task_test.rs"
